services:
  qemu-gui:
    build: .
    restart: always
    privileged: true
    ports:
      - "80:3000"
      - "5800-5999:5800-5999"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt
      - vms:/usr/src/app/vms
      - disks:/usr/src/app/disks
volumes:
  vms: {}
  disks: {}
